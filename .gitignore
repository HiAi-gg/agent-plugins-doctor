node_modules/
dist/
*.log
# The complex-plugin fixture intentionally ships a stray root file so the
# structure-extra-files rule (DOC-5003) has something to report; keep it tracked.
!tests/fixtures/complex-plugin/notes.log
.DS_Store
Thumbs.db
.env
.env.local
coverage/
.bob/
*.tsbuildinfo
# npm pack artifacts from the external-install E2E test (cleaned up by the test)
*.tgz

# Local agent tooling configuration (not part of the product)
.opencode/

# Internal development files (not for public release)
MEMORY.md
checkpoint.md
notes.md

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
